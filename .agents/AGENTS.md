# Project Rules

- Automatically push changes to the GitHub repository: After completing a task that modifies files, you must automatically add, commit, and push the changes to GitHub (`git add .`, `git commit -m "..."`, `git push origin main`) without waiting for explicit user permission.
